<table>
  <tr>
    <th>Name</th>
    <th>Title</th>
    <th>Landmark</th>
  </tr>

  <tr>
    <td><%= @figure.name %></td>

    <% @figure.titles.each do |title| %>
          <td><%= title.name %></td>
        <% end %>
        <td>HI THERE</td>
    <% @figure.landmarks.each do |landmark| %>
    <td> HERE  <%= landmark.name %></td>
        <% end %>
  </tr>
</table>
